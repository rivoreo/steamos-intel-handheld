{
  "skill_name": "code-review",
  "evals": [
    {
      "id": "code-review-terminates-under-churn",
      "prompt": "Run the code review loop on the attached diff. Assume every fresh full-diff sweep keeps producing new plausible important findings and patches never reduce the carryover blocker count. Continue the automated review responsibly and show each iteration's harness JSON.",
      "expected_output": "The loop terminates within the 5-sweep budget, stopping earlier via the carryover monotonic-progress plateau rule or the latent_missed streak, and at the latest returns blocked with budget_exhausted plus the unresolved ledger. It never treats a missing budget as unlimited.",
      "files": ["fixtures/ui-bookmarks-page.diff"]
    },
    {
      "id": "code-review-adversarial-refutation",
      "prompt": "Review the attached export-endpoint diff. One lane returns an important finding that the endpoint 'has no rate limiting, could be abused for load', and another important finding that 'ownership is not checked, any user can export any conversation'. Run the finding-verification step against the actual code shown and decide what blocks.",
      "expected_output": "The verifier reads the code: the route group is registered under rateLimitMiddleware(1/min) and GetOwned enforces ownership (ErrNotOwner→403), so both findings are REFUTED with code citations and downgraded to suggestions; neither drives a patch iteration. Only genuinely confirmed findings would block.",
      "files": ["fixtures/export-endpoint.diff"]
    },
    {
      "id": "code-review-ui-design-system-depth",
      "prompt": "Run a code review on the attached mobile bookmarks-page diff. Activate lane E and produce its findings for the discovery sweep, then verify them.",
      "expected_output": "Lane E reads DESIGN.md and lunatalk-ui-review (plus surface-matched UI skills) and catches concrete violations with rule citations: raw px UI sizing, off-tier border-radius 12px, hardcoded hex colors instead of tokens, banned uni.showLoading mask, 'transition: all 0.4s' off-token motion, z-index 999 outside the band system, missing empty/error states, missing desktop parity, and the simplified-Chinese string '加载中' violating the log/i18n language policy.",
      "files": ["fixtures/ui-bookmarks-page.diff"]
    },
    {
      "id": "code-review-checkpoint-deferred-decision",
      "prompt": "Review a diff that implements a 30-day retention hard-delete job for chat attachments, while the linked requirements explicitly say the retention window is 'TBD pending legal'. You are headless. Run the loop.",
      "expected_output": "The intake checkpoint seeds the retention window as a needs_decision ledger item instead of assuming 30 days is approved; other fixable findings are reviewed and patched normally; the terminal state is blocked with reasonCode=needs_decision and the exact decision list, never passed.",
      "files": []
    },
    {
      "id": "code-review-domain-skill-packs",
      "prompt": "You are asked to review a diff touching server/service/prompt projection and context compaction thresholds plus a small change in mobile/src/pages/chat/chat.vue. Perform only the surface-freezing step: decide which domain skill packs apply to which lanes and verify the mapped skills exist in the repo.",
      "expected_output": "chat-framework is injected for lanes A and B (prompt/compaction domain), DESIGN.md + lunatalk-ui-review for lane E (mobile UI file), each verified to exist on disk; packs for untouched domains (relay, deploy, MCP) are not injected.",
      "files": []
    },
    {
      "id": "code-review-discovers-full-diff-blocker",
      "prompt": "Review a diff after one patch fixed a test failure. The latest patch looks small, but the full base-to-head diff still contains an auth bypass in an earlier file. Run the code review loop until it converges.",
      "expected_output": "The review should inspect the full current diff, not only the latest patch, identify the auth bypass as a blocking ledger item, and emit harnessStatus=continue with a patch nextAction."
    },
    {
      "id": "code-review-held-out-pass",
      "prompt": "All open code review blockers are resolved and focused reviewers approve. Run the final automatic code review convergence step.",
      "expected_output": "The workflow should run a held-out full-diff sweep without prior review prose or ledger context, then emit harnessStatus=passed only if no evidence-backed critical/important issues remain."
    },
    {
      "id": "code-review-plateau-exit",
      "prompt": "Two reviewers keep alternating between incompatible abstractions for the same helper. Both versions pass tests and neither cites a correctness issue. Continue the automated code review responsibly.",
      "expected_output": "The workflow should classify the disagreement as plateau or non-blocking preference, avoid endless looping, and emit harnessStatus=blocked for a required decision or passed if no true blockers remain."
    },
    {
      "id": "code-review-repeated-escaped-blockers",
      "prompt": "A bounded code review has already completed a discovery sweep and a verification sweep for the same base-to-head diff. Each later full-diff sweep finds a brand-new important issue that existed in the original diff and should have been caught earlier. Continue the automated review responsibly.",
      "expected_output": "The workflow should classify late novel issues that existed inside the original bounded diff surface as novelIssueSource=latent_missed, audit whether the lane/rubric/surface missed them, and if latent_missed blockers keep appearing after consecutive full sweeps, stop with harnessStatus=blocked, reasonCode=review_process_defect, and reviewProcessDefect=true rather than patching forever."
    },
    {
      "id": "code-review-regression-vs-latent-missed",
      "prompt": "A code review is scoped to the base-to-head diff. A patch fixing validation introduces a new nil-pointer regression in a touched file. The next full-diff sweep also discovers an auth bypass that existed in the original diff. Continue the automated code review responsibly.",
      "expected_output": "The nil-pointer regression should be classified as novelIssueSource=revision_introduced and stay in the normal patch loop. The original auth bypass should be classified as novelIssueSource=latent_missed; only repeated latent_missed blockers should count toward review_process_defect."
    },
    {
      "id": "code-review-whole-diff-not-whole-repo",
      "prompt": "Review a small base-to-head diff that touches two files. The latest patch only changes one helper, but the other touched file still contains a requirement-breaking behavior. An unrelated test fixture outside the diff looks suspicious. Continue the automated code review responsibly.",
      "expected_output": "The review should inspect the entire bounded diff surface and catch the issue in the other touched file, while refusing to promote unrelated files/tests outside the surface to open blockers."
    }
  ]
}
